<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>Créer un compte - Express Brains</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- this is a link to the css file -->
    <link rel="stylesheet" href="/style.css" />

    <!-- remember to include custom fonts - if you are using them -->
    <link href="https://fonts.googleapis.com/css2?family=Zilla+Slab:wght@700&display=swap" rel="stylesheet">

  </head>
  <body class="container">
    <%- include('_header') %>
    <div class="row justify-content-md-center m-4">
        <h1 class="text-uppercase text-primary m-4 text-center">Créer un compte</h1>
        <form method="post" action="">
            
            <div class="row mb-3">
                <div class="col-sm-5 text-end">
                    <label for="pseudo" class="form-label">Pseudo : </label>
                </div>
                <div class="col-sm-7">
                    <input type="text" id="pseudo" name="pseudo" class="form-control <%= typeof messages != 'undefined' && typeof messages['pseudo'] != 'undefined' ? 'is-invalid' : '' %>">
                    <% if (typeof messages != 'undefined' && typeof messages['pseudo'] != 'undefined') { %>
                        <div class="text-center" style="color: red"><%=messages['pseudo'] %></div>
                    <% } %>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-sm-5 text-end">
                    <label for="email" class="form-label">Email </label>
                </div>
                <div class="col-sm-7">
                    <input type="email" id="email" name="email" class="form-control <%= typeof messages != 'undefined' && typeof messages['email'] != 'undefined' ? 'is-invalid' : '' %>">
                    <% if (typeof messages != 'undefined' && typeof messages['email'] != 'undefined') { %>
                        <div class="text-center" style="color: red"><%=messages['email'] %></div>
                    <% } %>
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-5 text-end">
                    <label for="password" class="form-label">Mot de passe </label>
                </div>
                <div class="col-sm-7">
                    <input type="password" id="password" name="password" class="form-control <%= typeof messages != 'undefined' && (typeof messages['password'] != 'undefined' || typeof messages['confirmationPassword'] ) != 'undefined' ? 'is-invalid' : '' %>">
                    <% if (typeof messages != 'undefined' && typeof messages['password'] != 'undefined') { %>
                        <div class="text-center" style="color: red"><%=messages['password'] %></div>
                    <% } %>
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-sm-5 text-end">
                    <label for="confirmationPassword" class="form-label">Confirmer le mot de passe </label>
                </div>
                <div class="col-sm-7">
                    <input type="password" id="confirmationPassword" name="confirmationPassword" class="form-control <%= typeof messages != 'undefined' && typeof messages['confirmationPassword'] != 'undefined' ? 'is-invalid' : '' %>">
                    <% if (typeof messages != 'undefined' && typeof messages['confirmationPassword'] != 'undefined') { %>
                        <div class="text-center" style="color: red"><%=messages['confirmationPassword'] %></div>
                    <% } %>
                </div>
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-primary">Créer un compte</button>
            </div>
        </form>
    </div>
    <%- include('_footer') %>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
  </body>
</html>
